# Backend Dockerfile — FastAPI + PuLP
FROM python:3.11-slim

WORKDIR /app

# Installer les dépendances système pour CBC solver
RUN apt-get update && apt-get install -y --no-install-recommends     gcc     g++     coinor-cbc     && rm -rf /var/lib/apt/lists/*

# Copier et installer dépendances Python
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copier le code
COPY . .

# Exposer le port
EXPOSE 8000

# Démarrer l'application
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
